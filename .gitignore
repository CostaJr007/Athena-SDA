# Secrets & environment
.env
.env.*
!.env.example
*.pem
credentials*.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local caches & logs
.cache/
*.log
.streamlit/secrets.toml

# Optional large local dumps (keep curated CSVs)
data/tle/*.txt
data/features/*.parquet
data/features/*.feather

# Frontend deps / build
node_modules/
src/frontend/node_modules/
src/frontend/dist/
*.local

# Backups / legacy trees / chat dumps (not product code)
leo-live-old/
chat_history_hackathon*.txt

# Redundant bulk mirrors (parquet/csv curated kept; raw GFZ re-downloadable)
data/history/epochs.csv
data/space_weather/Kp_ap_Ap_SN_F107_since_1932.txt

# Jupyter
.ipynb_checkpoints/

# Agent session dumps
docs/references/sessions/

# SPLID benchmark dataset (4.5 GB download; partial copy — never commit)
data/splid/

# Trained model artifacts + heavy data (regenerable — prefer Git LFS / external
# storage over versioning these binaries in the repo).
models/*.joblib
data/history/*.parquet
data/space_weather/*.parquet

# Generated / derived artifacts (regenerated by scripts/*).
data/alerts/reports/*.html
data/alerts/actions.jsonl
data/alerts/alert_state.json
data/alerts/validation_health.json
src/frontend/public/reports/quant_*.html

# Daily ingest products (regenerable; keep README)
data/daily/logs/
data/daily/tle_*.csv
data/daily/tle_*.meta.json

