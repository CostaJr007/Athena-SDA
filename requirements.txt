# Athena-SDA — pin lower bounds; topology uses canonical proxy unless ATHENA_USE_RIPSER=1
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
statsmodels>=0.14,<1
scikit-learn>=1.3,<2
xgboost>=2.0,<3
scikit-fuzzy>=0.4.2,<0.6
networkx>=3.0,<4
joblib>=1.3,<2
requests>=2.31,<3
python-dotenv>=1.0,<2
ibm-watsonx-ai>=1.0,<3
# Real TLE / HF ingest (anomaly monitor)
datasets>=2.16,<4
pyarrow>=14.0,<20
matplotlib>=3.7,<4
# Optional (topology / Space-Track API) — homology_mode recorded in model meta
# spacetrack>=0.16
# duckdb>=0.10
# ripser>=0.6
# sgp4>=2.22
